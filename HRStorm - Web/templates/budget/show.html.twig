{% extends '/employee/index.html.twig' %}

{% block title %}Budget{% endblock %}

{% block content %}
    <h1>Budget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ budget.id }}</td>
            </tr>
            <tr>
                <th>Budget</th>
                <td>{{ budget.budget }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ budget.date ? budget.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Prime</th>
                <td>{{ budget.prime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_budget_index') }}">Retour au liste</a>

   <!-- <a href="{{ path('app_budget_edit', {'id': budget.id}) }}">Modifier</a>-->

    {{ include('budget/_delete_form.html.twig') }}
{% endblock %}
