
{% extends "/employee/index.html.twig" %}


{% block title %}Competence {% endblock %}

{% block content %}
   <div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="">Gestion Des Compétences</h1>
            <div class="text-right">
                <a href="{{ path('app_competence_new') }}" class="btn btn-primary">Create new</a>
            </div>
            <ul class="nav nav-tabs">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_evaluation_index') }}">Evaluations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_poste_index') }}">Fiches des postes</a>
                </li>
            </ul>
        </div>
    </div>
</div>
 <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for competence in competences %}
            <tr>
                <td>{{ competence.Nom }}</td>
                <td>{{ competence.Description }}</td>
                <td>
                    <a href="{{ path('app_competence_show', {'id': competence.id}) }}">show</a>
                    <a href="{{ path('app_competence_edit', {'id': competence.id}) }}">edit</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
